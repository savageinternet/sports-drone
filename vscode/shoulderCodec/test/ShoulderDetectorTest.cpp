#include "catch.hpp"

#include "ShoulderDetector.hpp"

using namespace std;

SCENARIO("ShoulderDetectorTest", "[ShoulderDetectorTest]" ) {
    GIVEN("stuff") {
        WHEN("thing") {
            THEN("blel") {
                REQUIRE(1 + 1 == 2);
            }
        }
    }
}
