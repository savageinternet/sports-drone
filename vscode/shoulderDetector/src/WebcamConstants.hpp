#pragma once

class WebcamConstants {
public:
    /*
     * Set the camera width, height, and framerate here.
     */
    const static int CAMERA_WIDTH = 1280;
    const static int CAMERA_HEIGHT = 720;
    const static int CAMERA_FPS = 30;
};
